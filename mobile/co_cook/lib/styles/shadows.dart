import 'package:flutter/material.dart';

class CustomShadows {
  const CustomShadows();

  static const Shadow text = Shadow(
    color: Color.fromARGB(255, 0, 0, 0),
    offset: Offset(1, 1),
    blurRadius: 30.0,
  );
}
