import 'package:flutter/material.dart';

class CustomShadows {
  const CustomShadows();

  static const Shadow text = Shadow(
    color: Color.fromARGB(189, 0, 0, 0),
    offset: Offset(0, 0),
    blurRadius: 40.0,
  );
}
